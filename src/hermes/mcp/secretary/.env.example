# secretary-mcp 自己的密鑰模板 — 只有這個 MCP server 進程會讀，router 跟 Hermes
# 都不需要、也不會看到內容。這份是「樣板」，本身留空/不填也沒關係。
#
# Docker 部署（本 repo 的 container_manager.py）：房間第一次建立 container 時，
# router 的 _ensure_mcp_seed() 會把這份 .env.example 複製成該房間自己的
# data/<room_id>/mcp/secretary/.env——之後要改哪個房間的密鑰，直接編輯那個房間
# 自己的 .env（docker restart 生效），不影響其他房間，也不需要改這份樣板或根目錄
# .env。server.mjs 啟動時用 process.loadEnvFile() 自動讀取同目錄的 .env；
# Dockerfile.hermes 不會把任何 .env 烤進 image（見根目錄 .dockerignore 的 **/.env）。
#
# 獨立 VM 部署（見 README.md「首次安裝」）：把這個檔案複製成 .env，跟 server.mjs
# 放在同一個目錄即可，不需要另外 export 到 ~/.profile。

# Google Places API（新版）金鑰，maps_search / maps_details 工具需要。
# 留空則這兩個工具回設定錯誤，不影響其他工具。
# 取得方式：https://console.cloud.google.com/ 啟用 "Places API (New)"
# GOOGLE_MAPS_API_KEY=
