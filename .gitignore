# --- Python ---
__pycache__/
*.py[codz]
build/
dist/
*.egg-info/

# --- 型別檢查／lint／測試 ---
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# --- 虛擬環境 ---
.venv/

# --- Node（src/hermes/mcp/ 下的 package.json 是有 commit 的樣板；
#     node_modules 只烤進 Dockerfile.hermes 的 image，不落地） ---
node_modules/

# --- 編輯器 ---
.vscode/
.idea/

# --- macOS ---
.DS_Store

# --- 本專案 runtime／私人設定 ---
.env
# .env 的任何變體（手動備份 .env.bak-<date>、Finder 複製出的「.env copy」、.env.local、
# .env~…）一樣含 secret；只有 .env.example 範本要進版控
.env?*
!.env.example
data/
# One-shot migration backups (scripts/migrate_room_keys.py; cp -a data data.bak-<date>).
data.bak-*/
tmp/
CLAUDE.local.md
# 本機專用的 compose 覆寫（port mapping、掛載差異因機器而異，如 cloudflared ingress
# 轉到不同 port）；docker compose 會自動疊上，但不進版控
docker-compose.override.yml

# .claude/ 底下只有 rules/（path-scoped 的 agent 規則）與 skills/（project-level
# skills）要進版控，其餘（settings.local.json、快取等）是個人／機器本地的
.claude/*
!.claude/rules/
!.claude/skills/