services:
  webhook_router:
    build: .
    volumes:
      - ./data:/app/data
      - ./src/hermes:/app/hermes-templates:ro
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - LINE_CHANNEL_SECRET=${LINE_CHANNEL_SECRET}
      - LINE_CHANNEL_ACCESS_TOKEN=${LINE_CHANNEL_ACCESS_TOKEN}
      - HOST_DATA_DIR=${PWD}/data
      - HERMES_TEMPLATES_DIR=/app/hermes-templates
      - HERMES_IMAGE=${HERMES_IMAGE}
      - HERMES_API_SERVER_KEY=${HERMES_API_SERVER_KEY}
      - LLM_BASE_URL=${LLM_BASE_URL}
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_MODEL=${LLM_MODEL}
      - GOOGLE_OAUTH_PUBLIC_URL=${GOOGLE_OAUTH_PUBLIC_URL:-}
      - GOOGLE_OAUTH_GATE=${GOOGLE_OAUTH_GATE:-true}
    ports:
      - "8000:8000"
    networks:
      - hermes_global_net
    restart: always

networks:
  hermes_global_net:
    name: hermes_global_net
    external: true
